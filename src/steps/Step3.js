import React, { Component } from "react";
import strings from "../locale/strings.json";

class Step3 extends Component {
  state = {};

  render() {
    const { currentLang, gotoPrev, gotoNext } = this.props;

    return (
      <>
        <h2>Überschrift bla bla</h2>
      </>
    );
  }
}

export default Step3;
